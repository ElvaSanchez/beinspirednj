import type { Program } from "@/lib/content";
import { PlaceholderImage } from "./PlaceholderImage";
import { Button } from "./Button";
export function ProgramCard({ program, index, expanded = false }: { program: Program; index: number; expanded?: boolean }) { return <article id={expanded ? program.id : undefined} className={`program-row ${index % 2 ? "reverse" : ""}`}><PlaceholderImage id={expanded ? `programs/${program.id}` : `home/program-${program.id}`}/><div><p className="eyebrow">0{index + 1} / Pathways to possibility</p><h3>{program.title}</h3>{expanded && <><p className="lead">{program.tagline}</p><p>{program.description}</p><details className="program-details"><summary>View Program Details <span aria-hidden="true">+</span></summary><h4>Program Focus</h4><ul>{program.focus.map(focus => <li key={focus}>{focus}</li>)}</ul><Button href="/contact?topic=programs">Ask About This Program</Button></details></>} {!expanded && <Button href={`/programs#${program.id}`}>Learn More</Button>}</div></article>; }
